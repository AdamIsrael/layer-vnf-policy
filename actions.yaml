set-policy:
    description: "Set a user policy"
    params:
        user_id:
            description: "The user_id to modify the policy of"
            type: integer
            default: 0
        bw:
            description: ""
            type: integer
            default: 0
        qos:
            description: ""
            type: integer
            default: 0
    required:
        - user_id
        - bw
        - qos
